apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: monitoring
        applabel: app.kubernetes.io/version=0.22.2
        appkind: statefulset
    chaosServiceAccount: pod-io-stress-sa
    experiments:
        - name: pod-io-stress-alertmanager-kube-prometheus-stack-alertmanager-config-reloader
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: config-reloader
