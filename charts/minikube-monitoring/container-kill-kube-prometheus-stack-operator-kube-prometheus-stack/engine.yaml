apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: monitoring
        applabel: chart=kube-prometheus-stack-20.0.1
        appkind: deployment
    chaosServiceAccount: container-kill-sa
    experiments:
        - name: container-kill-kube-prometheus-stack-operator-kube-prometheus-stack
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: kube-prometheus-stack
