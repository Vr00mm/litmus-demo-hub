apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-network-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: monitoring
        applabel: app.kubernetes.io/name=kube-state-metrics
        appkind: deployment
    chaosServiceAccount: pod-network-loss-sa
    experiments:
        - name: pod-network-loss-kube-prometheus-stack-kube-state-metrics-kube-state-metrics
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: kube-state-metrics
