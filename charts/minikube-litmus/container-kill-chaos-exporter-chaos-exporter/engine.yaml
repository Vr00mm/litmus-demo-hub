apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: litmus
        applabel: app=chaos-exporter
        appkind: deployment
    chaosServiceAccount: container-kill-sa
    experiments:
        - name: container-kill-chaos-exporter-chaos-exporter
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: chaos-exporter
