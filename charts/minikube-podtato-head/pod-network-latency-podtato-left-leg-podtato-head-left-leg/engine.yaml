apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-network-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: podtato-head
        applabel: app.kubernetes.io/component=podtato-head-left-leg
        appkind: deployment
    chaosServiceAccount: pod-network-latency-sa
    experiments:
        - name: pod-network-latency-podtato-left-leg-podtato-head-left-leg
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: podtato-head-left-leg
