apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: podtato-head
        applabel: app.kubernetes.io/component=podtato-head-left-arm
        appkind: deployment
    chaosServiceAccount: pod-io-stress-sa
    experiments:
        - name: pod-io-stress-podtato-left-arm-podtato-head-left-arm
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: podtato-head-left-arm
