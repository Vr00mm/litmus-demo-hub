apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: oidc-provider
        applabel: app.kubernetes.io/instance=oidc-dex
        appkind: deployment
    chaosServiceAccount: pod-cpu-hog-exec-sa
    experiments:
        - name: pod-cpu-hog-oidc-dex-dex
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: dex
