apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata:
    name: nginx-chaos
    namespace: default
spec:
    engineState: active
    appinfo:
        appns: ingress-nginx
        applabel: app.kubernetes.io/component=controller
        appkind: deployment
    chaosServiceAccount: pod-cpu-hog-exec-sa
    experiments:
        - name: pod-cpu-hog-ingress-nginx-controller-controller
          spec:
            components:
                env:
                    - name: TARGET_CONTAINER
                      value: controller
